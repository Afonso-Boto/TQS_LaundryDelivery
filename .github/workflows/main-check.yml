name: Main check

on:
  pull_request:
    branches: [ main ]

jobs:
  verify-qourier:
    runs-on: ubuntu-latest

    steps:
    - name: Go to directory
      run: cd Qourier/qourier_app
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0    # Shallow clones should be disabled for a better relevancy of analysis
    - name: Set up OpenJDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'zulu'
        cache: maven
      run: mvn -B verify
    - name: Test report
      uses: dorny/test-reporter@v1
      if: always()
      with:
          name: Maven tests
          path: Qourier/qourier_app/target/surefire-reports/TEST-*.xml
          reporter: java-junit
          fail-on-error: true

  verify-laundryathome:
    runs-on: ubuntu-latest
    
    steps:
    - name: Go to directory
      run: cd LaundryAtHome/LaundryPlatform
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0    # Shallow clones should be disabled for a better relevancy of analysis
    - name: Set up OpenJDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'zulu'
        cache: maven
      run: mvn -B verify
    - name: Test report
      uses: dorny/test-reporter@v1
      if: always()
      with:
          name: Maven tests
          path: LaundryAtHome/LaundryPlatform/target/surefire-reports/TEST-*.xml
          reporter: java-junit
          fail-on-error: true