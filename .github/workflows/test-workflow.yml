name: Test

on:
  push:
    branches: [ ci-setup ]

jobs:
  verify:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: Qourier/qourier_app

    steps:
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0    # Shallow clones should be disabled for a better relevancy of analysis
    - name: Google Java Format enforcer
      uses: axel-op/googlejavaformat-action@v3
      with:
        files: "**/TestMe.java"
        args: "--aosp --skip-sorting-imports --skip-removing-unused-imports --set-exit-if-changed"
    - name: Google Java Format enforcer
      uses: axel-op/googlejavaformat-action@v3
      with:
        commitMessage: Google Java Format import ordering
        files: "**/TestMe.java"
        args: "--fix-imports-only"